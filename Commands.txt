Hey hi


version 1

version 2s

to see staged files 
$git diff --staged filename

Unstage files command
$git reset file name
$git reset .


reset physical file 
$git checout file name           // only perticuler file will be update
$git checout .              // all files

---------------------
$ls -la            // list all files available in folder if .git exist

barnches
-----------
git branch  // list all branches
git branch branch-name    // create branch

git checkout Branch-name  // switch branch 

--------------------merge branch

